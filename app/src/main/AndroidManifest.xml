<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.defate.mac.androidcommondemo">
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name=".MainActivity"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>


        <activity android:name=".samples.ScpActivity">

        </activity>

        <activity android:name=".ToolBarActivity" />

        <activity android:name=".samples.BlurredActivity">
        </activity>

        <activity android:name=".samples.PermissionActivity">

        </activity>

        <activity android:name=".WidgetActivity">

        </activity>

        <activity android:name=".agentwebs.AgentWebActivity">
        </activity>

        <activity android:name=".agentwebs.AgentFragmentAcitivity">

        </activity>

        <activity android:name=".rxjavas.RxDemoActivity">

        </activity>

        <activity android:name=".materials.CollapsingToolbarActivity"
                  android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity android:name=".materials.NormalViewsActivity">

        </activity>

        <activity android:name=".rxjavas.RxjavaWithAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>